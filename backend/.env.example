# Database Configuration
# Connection URL format: mysql+pymysql://user:password@host:port/database_name
DATABASE_URL=mysql+pymysql://counselor_app:counselor_pass@localhost/career_counselor

# Alternative individual database variables (used if DATABASE_URL not set)
DB_USER=counselor_app
DB_PASS=counselor_pass
DB_HOST=localhost
DB_PORT=3306